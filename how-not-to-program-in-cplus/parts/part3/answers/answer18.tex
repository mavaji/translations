\section{}
\paragraph{}\label{answer:18}
مسأله این است که \lr{\texttt{tmp\_name}} اشاره‌گری به متغیر محلی \lr{\texttt{name}} برمی‌گرداند. وقتی تابع تمام می‌شود، تمام متغیرهای محلیِ غیرایستا، از بین می‌روند. این قضیه شامل \lr{\texttt{name}} هم می‌شود. بنابراین، اشاره‌گری که برگردانده شد، به مکانی تصادفی و تخصیص‌نیافته از حافظه اشاره می‌کند. فراخوانی بعدیِ تابع، احتمالاً آن فضا را پاک می‌کند و \lr{\texttt{a\_name}} را کاملاً عجیب و غریب می‌سازد. راه‌حل این مشکل این است که \lr{\texttt{name}} را به صورت \lr{\texttt{static}} تعریف کنیم. (برنامه \ref{prog:59} مشکلی مشابه دارد).