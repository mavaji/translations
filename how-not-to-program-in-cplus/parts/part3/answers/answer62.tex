\section{}
\paragraph{}\label{answer:62}
مشکل این جاست که توضیح:
\begin{LTR}
    %@formatter:off
        \begin{lstlisting}[style=C++Style]
            10 base = 5; /* Set the base of the triangle
        \end{lstlisting}
    %@formatter:on
\end{LTR}

بسته نمی‌شود. لذا عبارت زیرِ خود را هم به عنوان توضیح دربرمی‌گیرد:
\begin{LTR}
    %@formatter:off
        \begin{lstlisting}[style=C++Style]
            10 base = 5; /* Set the base of the triangle
            11 height = 2; /* Initialize the height */
        \end{lstlisting}
    %@formatter:on
\end{LTR}

از این جا به راحتی می‌توان فهمید که چرا \lr{\texttt{height}} مقداردهی نشده است.