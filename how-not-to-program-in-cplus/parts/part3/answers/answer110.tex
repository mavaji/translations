\section{}
\paragraph{}\label{answer:110}
مقداردهی اولیه \lr{\texttt{log\_file}} می‌تواند \lr{\texttt{new}} را فراخوانی کند. البته \lr{\texttt{new}} جدید ما، از \lr{\texttt{log\_file}} استفاده می‌کند، لذا \lr{\texttt{log\_file}} ممکن است قبل از این که ساخته شود، مورد استفاده قرار گیرد که همه چیز را به هم می‌ریزد.