\section{}
\paragraph{}\label{answer:95}
مشکل در عبارت زیر است:
\begin{LTR}
    %@formatter:off
    \begin{lstlisting}[style=C++Style]
        24 sscanf(line, "%c %d", oper, value);
    \end{lstlisting}
    %@formatter:on
\end{LTR}

تابع \lr{\texttt{sscanf}} به عنوان آرگومانِ خود، اشاره‌گر می‌گیرد. (به یاد داشته باشید که \lr{\texttt{C}} آرگومان‌ها را برای داشتنِ نوعِ درست بررسی نمی‌کند).
