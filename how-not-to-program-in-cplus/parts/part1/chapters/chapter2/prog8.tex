\section[کاراکتر سرگشته]{کاراکتر سرگشته \protect\LTRfootnote{\lr{Mad Character}} (راهنمایی \ref{hint:131}، جواب \ref{answer:8})}
برنامه‌نویس مبتدی تصمیم گرفته تا دستور \lr{\texttt{if}} را با متغیرهای \lr{\texttt{char}} استفاده کند. برنامهٔ زیر، ساده، واضح و غلط است!

\begin{LTR}
    %@formatter:off
        \begin{lstlisting}[style=C++Style]
             /************************************************
             * Check the flag. *
             ************************************************/
             #include <iostream>

             int main()
             {
             	char ch; // The flag

             	ch = 0xFF; // Set the flag

             	// Check the flag
             	if (ch == 0xFF)
             		std::cout << "Success\n";
             	else
             		std::cout << "Fails\n";

             return (0);
             }
        \end{lstlisting}
    %@formatter:on
\end{LTR}

\begin{tcolorbox}
    \centering
    کنار اتاق کامپیوتر یک شرکت آلمانی پیدا شد:

    \lr{ATTENTION}

    \small\lr{This room is fullfilled mit special electronische equippment. Fingergrabbing and pressing the cnoeppkes from the computers is allowed for die experts only! So all the "lefthanders" stay away and do not disturben the brainstorming von here working intelligencies. Otherwise you will be out thrown and kicked anderswhere! Also: Please keep still and only watchen astaunished the blinkenlights.}
\end{tcolorbox}