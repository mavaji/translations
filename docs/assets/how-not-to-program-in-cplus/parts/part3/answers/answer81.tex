\section{}
\paragraph{}\label{answer:81}
برنامه‌نویس شک دارد که چیز مسخره‌ای دارد رخ می‌دهد وقتی داده \lr{\texttt{\#500}} در حال خوانده شدن است. او می‌خواهد یک نقطه‌انفصال\LTRfootnote{\lr{Breakpoint}} درست قبل از این که داده خوانده شود، قرار دهد. مشکل این است که اگر یک نقطه‌انفصال بالای \lr{\texttt{get\_data}} قرار دهد، باید \lr{\texttt{500}} دستور ادامه دیباگِر را انجام دهد تا به جایی که می‌خواهد برسد. بنابراین نقطه‌انفصال را در خط \lr{\texttt{seq = seq;}} می‌گذارد.