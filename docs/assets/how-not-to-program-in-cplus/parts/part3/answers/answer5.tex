\section{}
\paragraph{}\label{answer:5}
مشکل این است که نوع فایل را از نوع دودویی \lr{\texttt{(ios::bin)}} مشخص نکردیم. کتابخانهٔ زمانِ اجرای ویندوزِ مایکروسافت، کاراکتر خروجی را دست می‌زند و قبل از هر \lr{\texttt{<line\hyp{}feed(0xA)>}} یک \lr{\texttt{<carriage\hyp{}return(0xD)>}} درج می‌کند. این امر، \lr{\texttt{0D}} اضافی که قبل از کاراکتر \lr{\texttt{0A}} در فایل قرار دارد را توجیه می‌کند.
